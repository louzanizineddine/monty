#include "monty.h"
/**
 * stack_print - prints all the values on the stack, starting from the top
 * @stack: pointer to the head of the stack
 * @line_number: line number of the opcode
 * Return: void
 */

void stack_print(stack_t **stack, unsigned int line_number)
{
    
}